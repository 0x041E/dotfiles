##############################
# Backend
##############################
backend = "glx";
glx-no-stencil = true;
glx-copy-from-front = false;
#glx-no-rebind-pixmap = true;
#glx-swap-method = "undefined"
glx-swap-method = "buffer-age"; # auto detect. Alternatively can use "3" for triple buffer which is default for intel driver.
# glx-swap-method = "3"
# glx-use-gpushader4 = true; # only affects blurring

##############################
# Shadows
##############################
shadow = false;
# no-dnd-shadow = true;
# no-dock-shadow = true;
# clear-shadow = true;
# shadow-radius = 10;
# shadow-offset-x = -15;
# shadow-offset-y = -15;
# shadow-opacity = 0.9;
# shadow-radius = 10;
# shadow-offset-x = -5;
# shadow-offset-y = 0;
# shadow-opacity = 0.8;
# shadow-red = 0.11;
# shadow-green = 0.12;
# shadow-blue = 0.13;

xinerama-shadow-crop = true; # crop shadows between multiple monitors so shadow from edge of one window doesn't go over to next screen

#shadow-red = 0.34;
#shadow-green = 0.62;
#shadow-blue = 0.9;

shadow-exclude = [
    "! name~=''",
    "name = 'Notification'",
    "class_g = 'i3-frame'",
    "_NET_WM_STATE@:32a *= '_NET_WM_STATE_HIDDEN'",
    "_NET_WM_STATE@:32a *= '_NET_WM_STATE_STICKY'",
    "_GTK_FRAME_EXTENTS@:c"
];
shadow-ignore-shaped = true;

##############################
# Opacity
##############################

# menu-opacity = 0.9;
# inactive-opacity = 1;
# active-opacity = 1;
# frame-opacity = 1;
# inactive-opacity-override = false;

#ONLY XRENDER BACKEND
#alpha-step = 0.06;

#opacity-rule = [
#  "0:_NET_WM_STATE@[0]:32a = '_NET_WM_STATE_HIDDEN'",
#  "0:_NET_WM_STATE@[1]:32a = '_NET_WM_STATE_HIDDEN'",
#  "0:_NET_WM_STATE@[2]:32a = '_NET_WM_STATE_HIDDEN'",
#  "0:_NET_WM_STATE@[3]:32a = '_NET_WM_STATE_HIDDEN'",
#  "0:_NET_WM_STATE@[4]:32a = '_NET_WM_STATE_HIDDEN'",
#
#  "90:_NET_WM_STATE@[0]:32a = '_NET_WM_STATE_STICKY'",
#  "90:_NET_WM_STATE@[1]:32a = '_NET_WM_STATE_STICKY'",
#  "90:_NET_WM_STATE@[2]:32a = '_NET_WM_STATE_STICKY'",
#  "90:_NET_WM_STATE@[3]:32a = '_NET_WM_STATE_STICKY'",
#  "90:_NET_WM_STATE@[4]:32a = '_NET_WM_STATE_STICKY'"
#];

# inactive-dim = 0.1;
# inactive-dim-fixed = true;
blur-background = false;

#################################
# Fading
#################################

# Fade windows during opacity changes.
fading = true;
# fade-delta = 10;
fade-in-step = 0.05;
fade-out-step = 0.05;
# no-fading-destroyed-argb = true;
# fade-delta = 10;
# fade-in-step = 0.03;
# fade-in-step = 0.03;
# fade-out-step = 0.03;
# no-fading-openclose = true;
# fade-exclude = [ ];

#################################
# Other
#################################

# Try to detect WM windows and mark them as active.
mark-wmwin-focused = true;
# Mark all non-WM but override-redirect windows active (e.g. menus).
mark-ovredir-focused = true;
# Use EWMH _NET_WM_ACTIVE_WINDOW to determine which window is focused instead of using FocusIn/Out events.
# Usually more reliable but depends on a EWMH-compliant WM.
use-ewmh-active-win = true;
# Detect rounded corners and treat them as rectangular when --shadow-ignore-shaped is on.
detect-rounded-corners = true;

# Detect _NET_WM_OPACITY on client windows, useful for window managers not passing _NET_WM_OPACITY of client windows to frame windows.
# This prevents opacity being ignored for some apps.
# For example without this enabled my xfce4-notifyd is 100% opacity no matter what.
# CAN BREAK SOME PROGRAMS MAKING THEM NOT UNMINIMIZE KEEPING WINDOW CONTENTS TRANSPARENT(EMPTY WINDOW). KEEP DISABLED
# detect-client-opacity = true;

# Specify refresh rate of the screen.
# If not specified or 0, compton will try detecting this with X RandR extension.
refresh-rate = 0;

# Set VSync method. VSync methods currently available:
# none: No VSync
# drm: VSync with DRM_IOCTL_WAIT_VBLANK. May only work on some drivers.
# opengl: Try to VSync with SGI_video_sync OpenGL extension. Only work on some drivers.
# opengl-oml: Try to VSync with OML_sync_control OpenGL extension. Only work on some drivers.
# opengl-swc: Try to VSync with SGI_swap_control OpenGL extension. Only work on some drivers. Works only with GLX backend. Known to be most effective on many drivers. Does not actually control paint timing, only buffer swap is affected, so it doesn’t have the effect of --sw-opti unlike other methods. Experimental.
# opengl-mswc: Try to VSync with MESA_swap_control OpenGL extension. Basically the same as opengl-swc above, except the extension we use.
# (Note some VSync methods may not be enabled at compile time.)
# vsync = "drm"
# vsync = "opengl";
# vsync = "opengl-oml";
vsync = "opengl-swc"; # vsync handled by intel driver so there's no need to Vsync in compton it only adds extra overhead and can lead to more choppy behavior. Alternatively can use opengl-mswc or none.
# vsync = "opengl-mswc";
# vsync = "none";
# xrender-sync = true;
# vsync-use-glfinish = true;
xrender-sync-fence = true;
# Enable DBE painting mode, intended to use with VSync to (hopefully) eliminate tearing.
# ONLY WORK ON XRENDER BACKEND!
dbe = false;
# Painting on X Composite overlay window. Recommended.
# Using xrender sync fence fixes flickering so no need to draw on composite overlay
paint-on-overlay = false;

# Limit compton to repaint at most once every 1 / refresh_rate second to boost performance.
# This should not be used with --vsync drm/opengl/opengl-oml as they essentially does --sw-opti's job already,
# unless you wish to specify a lower refresh rate than the actual value.
# sw-opti = true;

# Unredirect all windows if a full-screen opaque window is detected, to maximize performance for full-screen windows, like games.
# Known to cause flickering when redirecting/unredirecting windows.
# paint-on-overlay may make the flickering less obvious.
unredir-if-possible = true;
# unredir-if-possible-delay = 5000;
# unredir-if-possible-exclude = [ ];

# Specify a list of conditions of windows that should always be considered focused.
focus-exclude = [
    "window_type = 'dock'",
    "window_type = 'desktop'"
];

# Use WM_TRANSIENT_FOR to group windows, and consider windows in the same group focused at the same time.
detect-transient = true;
# Use WM_CLIENT_LEADER to group windows, and consider windows in the same group focused at the same time.
# WM_TRANSIENT_FOR has higher priority if --detect-transient is enabled, too.
detect-client-leader = true;

#################################
#
# Window type settings
#
#################################

wintypes:
{
    tooltip =
    {
        # fade: Fade the particular type of windows.
        fade = true;
        # shadow: Give those windows shadow
        shadow = false;
        # opacity: Default opacity for the type of windows.
        opacity = 1.0; #0.9;
        # focus: Whether to always consider windows of this type focused.
        focus = true;
    };
    menu =
    {
        fade = false;
        shadow = true;
        focus = true;
    };
    dock =
    {
        fade = false;
        shadow = false;
        focus = true;
    };
};
